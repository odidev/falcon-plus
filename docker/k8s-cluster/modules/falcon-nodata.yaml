apiVersion: v1
kind: Service
metadata:
  namespace: odidev
  name: falcon-nodata
  labels:
    app: odidev
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 6090
  selector:
    name: falcon-nodata
---
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  namespace: odidev
  name: falcon-nodata
  labels:
    app: odidev
spec:
  replicas: 1
  template:
    metadata:
      labels:
        name: falcon-nodata
    spec:
      containers:
        - name: falcon-nodata
          image: registry.cn-hangzhou.aliyuncs.com/odidev/nodata:v0.3
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 6090
              name: http
              protocol: TCP
          volumeMounts:
            - mountPath: /odidev/nodata/config
              name: falcon-nodata-config
            - mountPath: /etc/localtime
              name: tz-config
      volumes:
        - flexVolume:
            driver: alicloud/nas
            options:
              path: /odidev/falcon-nodata/config
              server: xxx.cn-hangzhou.nas.aliyuncs.com
              vers: "4.0"
          name: falcon-nodata-config
        - hostPath:
            path: /etc/localtime
            type: ''
          name: tz-config